<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.k.mybatis.extend.BlogMapper">

 <!--   where
    <if test="blog.name != null">
        name  like   '%${blog.name}%'
    </if>-->
    <select id="selectList" resultType="org.k.mybatis.extend.Blog">

        select * from blog
    </select>
</mapper>
